# Credentials and persistent state — never commit.
# Contains: environment/.env, environment/data/userbot.session,
# environment/data/state.json, environment/signal-data/ (Signal link).
environment/

# Safety net in case someone creates a .env at project root by mistake
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Editor
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Local Claude Code permissions (machine-specific)
.claude/settings.local.json

# Per-project instructions for Claude Code — kept out of git by request.
# DO NOT remove this line unless the owner explicitly authorizes committing CLAUDE.md.
CLAUDE.md
